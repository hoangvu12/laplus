[package]
name = "spike-claude-protocol"
version = "0.0.0"
edition = "2021"
publish = false

# PROTOTYPE — throwaway. See README.md for the question this answers.
# Deliberately minimal deps: the point is to prove the protocol parses,
# not to pick the production stack (that's axum + tokio, per the handoff).

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
