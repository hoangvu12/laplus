[package]
name = "xtask"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

# Nothing. This crate runs `cargo`, reads files and counts bytes, and a
# dependency would have to earn its way into a build whose whole subject is
# what things weigh.
[dependencies]
